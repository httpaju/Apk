<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>AppZorix - App Store</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            text-align: center;
            background: linear-gradient(135deg, #ff9a9e, #fad0c4);
            margin: 0;
            padding: 0;
            overflow-x: hidden;
        }
        h1 {
            margin-top: 20px;
            animation: fadeIn 1s ease-in-out;
        }
        .app-list {
            display: flex;
            flex-wrap: wrap;
            justify-content: center;
            gap: 20px;
            padding: 20px;
        }
        .app-card {
            background: white;
            border-radius: 10px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
            padding: 15px;
            width: 250px;
            text-align: center;
            transform: scale(0.8);
            opacity: 0;
            transition: transform 0.3s ease, opacity 0.3s ease;
        }
        .app-card img {
            width: 100px;
            height: 100px;
            border-radius: 10px;
            transition: transform 0.3s ease;
        }
        .app-card:hover img {
            transform: scale(1.1);
        }
        .app-card:hover {
            transform: scale(1);
            box-shadow: 0 6px 12px rgba(0, 0, 0, 0.3);
        }
        .app-card a {
            display: inline-block;
            margin-top: 10px;
            padding: 10px;
            background: #ff758c;
            color: white;
            text-decoration: none;
            border-radius: 5px;
            transition: background 0.3s;
        }
        .app-card a:hover {
            background: #ff5470;
        }
        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(-20px); }
            to { opacity: 1; transform: translateY(0); }
        }
    </style>
</head>
<body>
    <h1>Welcome to AppZorix</h1>
    <p>Developed by AJ APPLICATIONS</p>
    <div class="app-list">
        <% apps.forEach((app, index) => { %>
            <div class="app-card" style="animation: fadeIn 0.5s ease-in-out <%= index * 0.2 %>s forwards;">
                <img src="<%= app.imageUrl %>" alt="<%= app.name %>">
                <h3><%= app.name %></h3>
                <p><%= app.description %></p>
                <a href="<%= app.downloadUrl %>">Download</a>
            </div>
        <% }) %>
    </div>
</body>
</html>
