<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>AppZorix - App Store</title>
    <link rel="icon" href="https://i.ibb.co/9m29bBsr/extracted-white-icon-1-removebg-preview.png" type="image/png">
    
    <style>
        /* General Styles */
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            background-color: #f4f4f4;
            text-align: center;
            animation: fadeIn 1s ease-in-out;
        }

        /* Drawer Styles */
        .menu-btn {
            position: fixed;
            top: 15px;
            left: 15px;
            background: #007bff;
            color: white;
            padding: 10px 15px;
            border: none;
            cursor: pointer;
            border-radius: 5px;
            font-size: 18px;
            z-index: 1001;
        }

        .drawer {
            position: fixed;
            top: 0;
            left: -250px;
            width: 250px;
            height: 100vh;
            background: rgba(0, 0, 0, 0.9);
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            transition: left 0.3s ease-in-out;
            z-index: 1000;
        }

        .drawer a {
            color: white;
            text-decoration: none;
            font-size: 24px;
            margin: 10px 0;
        }
        .drawer a:hover {
            text-decoration: underline;
        }

        .close-btn {
            position: absolute;
            top: 20px;
            right: 20px;
            background: red;
            color: white;
            padding: 10px;
            cursor: pointer;
            border-radius: 5px;
        }

        /* Footer */
        .footer {
            margin-top: 30px;
            padding: 10px;
            background: #333;
            color: white;
            font-size: 16px;
            animation: slideUp 1s ease-in-out;
        }
        .footer a {
            color: #ffcc00;
            text-decoration: none;
            font-weight: bold;
            margin-left: 10px;
        }
        .footer a:hover {
            text-decoration: underline;
        }

        /* Logo Container */
        .logo-container {
            display: flex;
            align-items: center;
            justify-content: center;
            background: #007bff;
            padding: 15px;
            border-radius: 10px;
            width: fit-content;
            margin: 20px auto;
            gap: 10px; /* Space between logo and text */
        }

        .logo-container img {
            width: 50px;
            height: 50px;
        }

        .logo-container h2 {
            color: white;
            margin: 0;
            font-size: 24px;
            font-weight: bold;
        }

        /* Search Bar */
        .search-bar {
            margin: 20px auto;
            max-width: 400px;
        }
        .search-bar input {
            padding: 10px;
            width: 70%;
            border: 1px solid #ccc;
            border-radius: 5px;
        }
        .search-bar button {
            padding: 10px;
            border: none;
            background: #007bff;
            color: white;
            cursor: pointer;
            border-radius: 5px;
        }

        /* App List */
        .app-list {
            display: flex;
            flex-wrap: wrap;
            justify-content: center;
            padding: 20px;
        }
        .app-card {
            width: 150px;
            margin: 10px;
            padding: 10px;
            background: white;
            border-radius: 10px;
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
            cursor: pointer;
        }
        .app-card img {
            width: 100%;
            border-radius: 5px;
        }

        /* Floating Donation Button */
        .donation-button {
            position: fixed;
            bottom: 20px;
            right: 20px;
            background: #ffcc00;
            color: black;
            padding: 10px 15px;
            text-decoration: none;
            border-radius: 5px;
            font-weight: bold;
        }
    </style>
</head>
<body>
    <!-- Menu Button -->
    <button class="menu-btn" onclick="toggleDrawer()">☰ Menu</button>

    <!-- Drawer -->
    <div id="drawer" class="drawer">
        <button class="close-btn" onclick="toggleDrawer()">✖ Close</button>
        <a href="https://appzorix-apk-upload.onrender.com" target="_blank">Upload Your Apk</a>
        <a href="https://www.instagram.com/aj_applications/" target="_blank">Follow us on Instagram</a>
    </div>

    <!-- Logo Container -->
    <div class="logo-container">
        <img src="https://i.ibb.co/DD0spV8M/extracted-white-icon-1.png" alt="AppZorix Logo">
        <h2>AppZorix</h2>
    </div>

    <!-- Search Bar -->
    <div class="search-bar">
        <form action="/" method="GET">
            <input type="text" name="search" placeholder="Search apps..." value="<%= searchQuery || '' %>">
            <button type="submit">Search</button>
        </form>
    </div>

    <!-- App List -->
    <div class="app-list">
          <% apps.forEach(app => { %>
            <div class="app-card" onclick="window.location.href='/app/<%= app._id %>'">
                <img src="<%= app.imageUrl %>" alt="<%= app.name %>">
                <h3><%= app.name %></h3>
            </div>
        <% }); %>
    </div>

    <!-- Floating Donation Button -->
    <a href="https://aj-t2s.ddnsking.com/donation" class="donation-button" target="_blank">
        💰 Support Us
    </a>

    <!-- Footer -->
    <div class="footer">
        AJ APPLICATIONS | <a href="https://www.instagram.com/aj_applications/" target="_blank">Follow us on Instagram</a>
    </div>

    <script>
        function toggleDrawer() {
            let drawer = document.getElementById("drawer");
            drawer.style.left = (drawer.style.left === "0px") ? "-250px" : "0px";
        }
    </script>
</body>
</html>
